public class TaskHandler {
 
   public enum OBJECT_TYPE {TASK, SUBTASK,NOT_DEFINED}
    public static void handleOnAfterInsert(String serStr){
     //   OBJECT_TYPE objectType;
    //    ID jobID = System.enqueueJob(new QuebleSynchronizatorBeforeInsert(JSON.serialize(Trigger.new),OBJECT_TYPE.TASK));
    }
 
	 
}